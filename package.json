{
    "name": "wheres-waldo-backend",
    "version": "1.0.0",
    "scripts": {
      "heroku-postbuild": "cd backend && npm install && npm run build"
    },
    "engines": {
        "node": ">=18.17.1"
      }
  }
  